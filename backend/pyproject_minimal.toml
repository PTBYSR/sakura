[project]
name = "sakura-backend"
version = "0.1.0"
description = "Backend for Sakura AI Concierge"
requires-python = ">=3.10"

dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "pymongo",
    "python-dotenv",
    "pydantic-settings",
    "jsonlines",
    "tqdm",
    "langchain-google-genai>=2.1.12",
]

